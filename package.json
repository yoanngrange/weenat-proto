{
  "name": "weenat-proto",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "homepage": ".",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && cp -r dist/. public/"
  },
  "devDependencies": {
    "playwright": "^1.60.0",
    "vite": "^5.2.0"
  },
  "dependencies": {
    "leaflet": "^1.9.4"
  }
}
